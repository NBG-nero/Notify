// import 'package:hive_flutter/hive_flutter.dart';

// import '../../models/models.dart';
// import '../../utilities/constants/constants.dart';
import '../base_model.dart';

class ViewNoteViewModel extends BaseModel {
  // saveNoteColor(String? id) {
  //   Note saveNote = Note(id: id, noteColor: selectedColor);
  //   var hiveBox = Hive.box<Note>(noteBox);
  //   hiveBox.put(saveNote.id, saveNote);
  //   // log(id!);
  //   return saveNote.id;
  // }
}
